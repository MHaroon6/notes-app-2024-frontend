"use client";

const AddButton = () => {
  return (
    <button className=" font-semibold rounded-full py-3 px-4 transition-all delay-100 ease-in-out custom-btn text-sm">
      New Note
    </button>
  );
};

export default AddButton;
